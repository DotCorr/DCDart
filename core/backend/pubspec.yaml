name: backend
description: >
  DC-IR -> LLVM IR -> object file (DCDART_SPEC.md §1). Plain hosted Dart,
  see core/docs/decisions/0006-toolchain-bootstrap-language.md.
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dc_ir:
    path: ../dc-ir
